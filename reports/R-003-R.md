# R-003-R · Reopened: offline-tolerant sync and AC-3.5 evidence

Task: tasks/T-003-backend.md (T-003-R) · Maker: Claude Code · Reviewer: Claude Web · Date: 2026-09-08

> **Headline: the offline defect was real, it had a specific cause, and the cause was not the
> one the reopening assumed.**
>
> No guard rejected the tap. Derivation never had a server dependency and still does not. What
> did have one was **queueing** — `queueNight` sat inside `if (data.user)`, and `auth.getUser()`
> is a network round trip. Offline that call rejected, an empty `catch` swallowed it, and the
> night was never queued at all. "Retry on next foreground" then had nothing to retry, and no
> foreground listener existed to retry it with.
>
> Three things changed: queueing is now network-free, a single `AppState` listener drains on
> foreground, and every path through the tap handler now writes a status line beside the button.
> 153 tests pass, 17 of them new; 12 of the 17 fail against the shipped `e1a3bdd4`, which is
> expected — they describe behaviour that build does not have.
>
> **DEF-003-01 itself is not fixed, and this report does not claim it is.** The checker's frame
> evidence — the button changed online, stayed solid black offline — shows the tap never reached
> the handler at all. The queue fix lives inside that handler. See §3.
>
> **Status: AWAITING_DEVICE_RUN** (D-040 as clarified 2026-09-08): the maker states each
> deliverable's state, the founder performs the device pass. **AC-3.5 remains unproven** — §7.

---

## 1. What changed

### The queue no longer needs a network to accept a night

`app/src/backend/sync.ts`. `queueNight(night, remoteCommitmentId)` became `queueNight(night)`,
and it now awaits exactly one thing: the local `enqueue`. The two fields it cannot know on a
phone with the radio off — `commitment_id`, which requires a server round trip, and
`device_clock_offset_min`, which requires a clock probe — are stamped at drain time instead.
Neither is part of the verdict, so neither can change what was derived on device.

A queued-but-unstamped night carries `commitment_id: 0` (`UNSTAMPED_COMMITMENT_ID`). Postgres
identities start at one, so that value is not merely a convention: a row still carrying it has
provably never been uploaded.

### `drainQueue` stopped throwing and stopped needing a round trip to decide

It now returns a `stoppedBecause` string for every ordinary failure — offline, signed out, not
configured, no local commitment — because offline is a normal state of this app, not an
exception. It also asks `auth.getSession()` (reads local storage) rather than `auth.getUser()`
(a request to `/auth/v1/user`). Asking the server whether you are signed in, in order to decide
whether to try the server, is a round trip spent to learn something already on disk.

### One `AppState` listener — the first lifecycle handling in the codebase

`app/App.tsx`. Fires only on `active`, calls the drain, removes itself on unmount. No
react-navigation, no `BackgroundFetch`, no `TaskManager`, no scheduler; a test asserts none of
those four packages appear in `dependencies`, so the constraint outlives this commit.

### Boot no longer mistakes "couldn't check" for "signed out"

The boot effect set `session = null` in a `catch` and then routed to sign-in on `session ===
null`. Offline, `getSession()` failing to confirm a session is not the same fact as there being
none, and the old code could not tell them apart — so a signed-in user opening the app without a
network was liable to be thrown back to sign-in. A `sessionKnown` flag now separates the two, and
sign-in is only forced when the absence of a session is positively established.

This was not in DEF-003-01, and I have not proven it fired on the founder's device. It is a
genuine offline defect found while tracing that path, and it is fixed rather than left.

### Feedback lives next to the control that produces it

`app/src/ui/HistoryScreen.tsx` and the tap handler. Every path through the handler sets a status
line in a `finally`, and it renders beside the button rather than at the top of the screen.

Read §3 before treating this as the DEF-003-01 fix. It is not one. On the checker's evidence the
handler was never entered, so a line written inside it would never have appeared either. This is
instrumentation that makes the next device run conclusive.

---

## 2. Screens and controls: what I could and could not verify

**I did not run the app.** This machine (Windows 11, D-013) has no JDK, no Android SDK and no
emulator; every build in this project has gone through EAS in the cloud. There is no path by
which I can tap a control and report what it displayed. D-040 assigns that pass to the founder;
my half is §5, stating each deliverable's state. Status is AWAITING_DEVICE_RUN.

What I can state, per control, is what the code now does — and what evidence exists for it:

| Screen · control | Behaviour after this change | Evidence |
|---|---|---|
| Home · **Check last night** | Sets `busy` first; label becomes "Reading…", and an adjacent line reads "Reading your watch…". On completion the adjacent line always carries a result. **On `e1a3bdd4` this button did not change offline at all**, and nothing here is known to change that — see §3. | Code + `finally` test; **DEF-003-01 open** |
| Home · status line (new) | Never empty after a tap. Set in `finally`, so no path — success, permission denied, read failure, thrown error — can leave it blank. | Test: "the note is set in a finally" |
| Home · error banner | Unchanged, still at the top. It is now a *supplement* to the adjacent line rather than the only signal. | Code |
| Home · 30-night list | Unchanged. Refreshed after every derivation, offline included. | Code |
| Home · Change my promise / Settings | Unchanged, untouched. | — |
| Sign-in | Unchanged. Accepts 6–10 digits, so the founder's change of `otp_length` 8 → 6 needs no code change. | `tests/otp-format.test.ts` |
| Settings · sign out / export / delete | Unchanged, untouched. AC-3.7 and AC-3.8 passed on device and nothing in this change touches those paths. | — |

The one thing a reader should not take from this table: that any of it has been seen working.
None of it has.

---

## 3. Root cause of the silent no-op

The task asked me to *name the guard that rejected the tap*, and whether the failure was
caught-and-ignored or never thrown. The honest answer is that the premise is wrong, and I would
rather say so than nominate a plausible culprit.

**No guard rejected the tap.** The only guard on that path is `disabled={p.busy}`, and the
handler's first statement is `setBusy(true)`. The net guard (`assertAllowedHost` /
`assertNoRawHealth`) is handed to the Supabase client alone; it is not a global `fetch` override
and cannot see, let alone reject, a button press. Nothing on the tap path calls it.

What I can prove, and what I cannot:

**Proven, caught-and-ignored — the queue.** In `e1a3bdd4`:

```ts
try {
  const { data } = await getSupabase().auth.getUser();   // network round trip
  if (data.user) {
    const remoteId = await ensureRemoteCommitment(c);     // network round trip
    await queueNight(outcome.stored, remoteId);           // ← only reachable online
    await drainQueue();
  }
} catch {
  // Offline, signed out, or refused: the night stays queued for the
  // next foreground (AC-3.6).
}
```

The comment asserts the night stays queued. It does the opposite: `queueNight` is inside the
`try`, after two network calls, so offline the rejection from `getUser()` jumps straight to a
`catch` that does nothing, and **nothing is ever enqueued**. AC-3.6 and AC-R2 were unreachable
by construction. Caught, ignored, and — worse — annotated with a claim that the reader would
have had no reason to doubt.

**Unproven — the missing "Reading…" frame.** I could not reproduce this by reading, and I will
not name a cause I cannot demonstrate. `setBusy(true)` is the first statement and `busy` is
wired to the label, so a tap that reaches the handler should repaint even offline.

I proposed a hypothesis here — that every signal was off-screen, above the fold from the
button — and **the checker rejected it with evidence that settles it.** In the founder's online
screenshot the button *itself* turned grey and its label changed to "Reading…", fully visible.
In the airplane-mode recording that same button stayed solid black across every frame. The
signal that matters was never off-screen; it was co-located with the finger all along.

So DEF-003-01 is **not** a rendering-position problem and **not** the same defect as the
out-of-scope layout item. What the evidence says is narrower and more uncomfortable: `busy`
never became true, which means **`setBusy(true)` never ran, which means the handler never
started.** The tap did not reach `onPress`.

**That cause is still unidentified, and nothing in this change is known to fix it.** The queue
fix is real and independently necessary — a night derived offline was never enqueued, and now is
— but it lives *inside* a handler that, on the evidence, was never entered. Fixing what happens
after the tap cannot fix a tap that does not arrive.

The status line I added is therefore not the fix; it is instrumentation. If the next run shows
"Reading your watch…" appear offline, the handler is being entered and the old build's silence
had some other explanation. If the line stays blank while the button stays black, the handler is
still not being entered and the fault is upstream of all of this — in the Pressable, the
ScrollView's touch handling, or the JS thread. Either outcome is conclusive, which is why AC-R1
must be photographed and why I am not claiming DEF-003-01 as fixed.

---

## 4. Did derivation itself have a server dependency?

**No. Only the upload did.** Stated plainly, as asked.

`deriveAndStoreNight` reaches exactly two things: the `HealthStore` (Health Connect, on-device)
and SQLite. It imports nothing from `src/backend` and calls no `fetch`. The verdict, the
revision and freeze rules, the wear-presence ratio and the RHR coherence check are all pure
TypeScript over local data. D-010 is intact and was never at risk.

This is now enforced rather than merely true: `tests/offline-sync.test.ts` fails if
`nightlyEngine.ts` or the `derive` layer gains an import from `src/backend` or `src/net`. The
property was previously a fact about the current call order; it is now a fact about the design.

**No design defect to escalate on this point.** The defect was that a *local* operation —
queueing — had been placed behind a *remote* one.

---

## 5. T-003 deliverables 1–9 (D-040)

| # | Deliverable | State |
|---|---|---|
| 1 | Migrations | **IMPLEMENTED**, applied, schema verified against the live database |
| 2 | RLS policies | **IMPLEMENTED**, proven 8/8 |
| 3 | Sign-in screen | **IMPLEMENTED**, proven on device 2026-09-08 |
| 4 | Sync, offline-tolerant | **PARTIAL → code-complete, unproven.** Queue network-free; foreground drain added; idempotent drain tested. Not seen working on a device, so not IMPLEMENTED. |
| 5 | Network guard extended | **IMPLEMENTED** in code and tests; the live half is AC-3.5, still unproven |
| 6 | Export | **IMPLEMENTED**, passed on device |
| 7 | Delete account | **IMPLEMENTED**, passed on device |
| 8 | Settings screen | **IMPLEMENTED** |
| 9 | Preview APK + report | **PARTIAL** — this report exists; the APK is gated on §8 |

---

## 6. Tests

| Gate | Command | Result |
|---|---|---|
| Full suite | `npm test` | **153 passed, 2 skipped**, 12 files (was 136 / 11) |
| derive | `npx vitest run derive` | 35/35 |
| netguard | `npx vitest run netguard` | 15/15 |
| eligibility | `npx vitest run eligibility` | 13/13 |
| Typecheck | `npx tsc --noEmit` | clean |
| Lint | `npm run lint` (`eslint .`) | clean, 0 warnings |
| RLS | `backend/tests/rls_denial.sql` | 8/8, unchanged — this change touches no SQL |

**17 new tests in `tests/offline-sync.test.ts`. 12 of them fail against `e1a3bdd4`**, verified by
stashing the three changed source files and re-running: the queue tests, the AppState tests, the
feedback tests and the drain-behaviour tests all go red. The 5 that pass against both are the
invariants that were already true (derive-layer purity, no banned dependencies, the queue being a
real SQLite table) and are there to stop them becoming false.

A gate that cannot fail is not a gate; these were checked, not assumed.

---

## 7. AC-3.5 — still not satisfied, and not fudged

Not done. It requires capturing a real signed-in session's outbound traffic, which requires the
device. The task is explicit that the guard's 15/15 against synthetic payloads does not satisfy
this AC, and I agree — it is a different claim. Nothing in this report should be read as
evidence for it.

Recommended method for the next run, cheapest first: sign in, tap Check last night, then read
the Supabase **API logs** for the project and paste the request bodies verbatim. If the log does
not show bodies, a proxy on the phone's Wi-Fi is the fallback. What must be shown absent: any
timestamp inside the sleep window, any HR value, any RHR value.

---

## 8. Build — stopped for confirmation (D-036 / D-038)

Not built. D-036 requires printing the authenticated identity and stopping before a command
touches an external account, org or project. Doing so:

```
$ npx eas-cli whoami
zenoho-expo-new
zenohohealth@gmail.com

Accounts:
• zenoho-expo-new (Role: Owner)
• zenoho-expo-news-team (Role: Owner)
```

`app/app.json` names owner `zenoho-expo-new`, project `e5b3950d-6ae6-4564-bbd8-f201bae20ae1`,
slug `zenoho2` — the CLI identity matches the project it would build. Two accounts exist, which
is exactly the ambiguity D-036 was written for after D-035.

Awaiting the founder's go-ahead to run `eas build --profile preview --platform android`. The
cold-install rehearsal follows the build, not this report.

---

## 9. Status, assumptions, risks

**Status: AWAITING_DEVICE_RUN.**

My first reading of D-040 — that the *maker* must exercise every screen, therefore NOT_DONE —
was wrong, and the checker corrected it. That reading makes the rule unsatisfiable, since this
machine has no device to exercise. D-040 requires a *human* pass: the maker states each
deliverable's state (§5), the founder runs the device. IN_REVIEW follows device evidence.

**Assumptions, each of which could be wrong:**

1. ~~That co-locating feedback addresses DEF-003-01.~~ **Withdrawn** — the checker's evidence
   (the button itself changed online and did not offline) shows the tap never reached the
   handler. DEF-003-01 is **not** fixed by this change and must not be reported as fixed. The
   status line is instrumentation that will make the next run conclusive either way.
2. That `getSession()` resolves offline from local storage rather than blocking on a refresh.
   Behaviour under an expired token with no network is asserted from the supabase-js contract,
   not observed.
3. That `commitment_id: 0` never collides. True for Postgres identity columns; it would stop
   being true if the column were ever backfilled by hand.

**Risks:**

- **The one that matters:** two of the three fixes are structurally verified but behaviourally
  unproven. The queue fix I would defend strongly — the old code path is plainly unreachable
  offline. The visual-feedback fix rests on a hypothesis I could not confirm.
- The `sessionKnown` boot change alters routing for signed-in users offline. It is a strict
  improvement in intent, and it is not covered by any automated test, because the boot effect
  is not reachable from vitest.
- Resend still delivers only to zenohohealth@gmail.com (D-039). Unchanged, and blocking for
  anyone but the founder.

**Recommended next step:** run **AC-R1 first** and photograph the button and the line beneath
it. Watch the button, not the line: if it greys to "Reading…" offline, the handler is being
entered and the queue fix is live. If it stays solid black as it did on `e1a3bdd4`, DEF-003-01
is untouched and the fault is upstream of everything in this change — in the Pressable, the
ScrollView's touch handling, or the JS thread — and that is the next thing to chase. Everything
else in the run depends on which of those two it is.

---

## 10. Files changed, every staged path (D-041)

Staged explicitly, no `git add -A`:

```
app/App.tsx                      local-first sync, unconditional queueing, AppState drain,
                                 offline-tolerant boot routing, unused import removed
app/src/backend/sync.ts          queueNight network-free; drain stamps commitment id and clock
                                 offset; drain never throws; idempotency documented
app/src/ui/HistoryScreen.tsx     status line beside the button; pendingSync prop
tests/offline-sync.test.ts       new: 17 tests, 12 of which fail against e1a3bdd4
reports/R-003-R.md               this report
```

Nothing else. No dependency was added, no schema changed, no SQL touched, no copy outside the
Home status line altered.
